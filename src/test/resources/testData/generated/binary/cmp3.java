public class Cmp3 {

    public static void given(Cmp3 arg) {
        if (arg.foo() == "") {

        }
    }

    public static void want() {
        boolean $$$bool$$$1 = (boolean) applyFunction.apply(Arrays.asList(1, (String) applyFunction.apply(Arrays.asList(2, arg.foo(), "entry", 0, Thread.currentThread().getId())) == "", "entry", 0, Thread.currentThread().getId()));
        applyFunction.apply(Arrays.asList(0, $$$bool$$$1, "entry", 0, Thread.currentThread().getId()));
        if ($$$bool$$$1) {
            applyFunction.apply(Arrays.asList(3, "block1", "block1", 0, Thread.currentThread().getId()));

        }
    }

    public String foo() {
        return "";
    }
}