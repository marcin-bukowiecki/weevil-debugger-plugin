public class Cmp2 {

    public static void given(Cmp2[] args) {
        if (args.length == 0) {

        }
    }

    public static void want() {
        boolean $$$bool$$$1 = (boolean) applyFunction.apply(Arrays.asList(1, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(2, ((Cmp2[]) applyFunction.apply(Arrays.asList(3, args, "entry", 0, Thread.currentThread().getId()))).length, "entry", 0, Thread.currentThread().getId())).toString()) == 0, "entry", 0, Thread.currentThread().getId()));
        applyFunction.apply(Arrays.asList(0, $$$bool$$$1, "entry", 0, Thread.currentThread().getId()));
        if ($$$bool$$$1) {
            applyFunction.apply(Arrays.asList(4, "block1", "block1", 0, Thread.currentThread().getId()));

        }
    }
}