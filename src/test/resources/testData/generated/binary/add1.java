class Add1 {

    public int given(int n) {
        return n + 11
    }

    public void want() {
        applyFunction.apply(Arrays.asList(2, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(0, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(1, n, "entry", 0, Thread.currentThread().getId())).toString()) + 11, "entry", 0, Thread.currentThread().getId())).toString()), "entry", 0, Thread.currentThread().getId()));
        throw returnException;
    }
}