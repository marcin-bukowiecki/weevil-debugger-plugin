class Add2 {

    public int given(int n, int m) {
        return n + 11 * m
    }

    public void want() {
        applyFunction.apply(Arrays.asList(4, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(0, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(1, n, "entry", 0, Thread.currentThread().getId())).toString()) + java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(2, 11 * java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(3, m, "entry", 0, Thread.currentThread().getId())).toString()), "entry", 0, Thread.currentThread().getId())).toString()), "entry", 0, Thread.currentThread().getId())).toString()), "entry", 0, Thread.currentThread().getId()));
        throw returnException;
    }
}