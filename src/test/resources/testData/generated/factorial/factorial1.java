class HelloWorld1 {

    public void given(int n) {
        if (n == 0 || n == 1) return 1;
        return n * factorial(n - 1);
    }

    public void want() {
        boolean $$$bool$$$1 = (boolean) applyFunction.apply(Arrays.asList(1, (boolean) applyFunction.apply(Arrays.asList(2, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(3, n, "entry", 0, Thread.currentThread().getId())).toString()) == 0, "entry", 0, Thread.currentThread().getId())) || (boolean) applyFunction.apply(Arrays.asList(4, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(5, n, "entry", 0, Thread.currentThread().getId())).toString()) == 1, "entry", 0, Thread.currentThread().getId())), "entry", 0, Thread.currentThread().getId()));
        applyFunction.apply(Arrays.asList(0, $$$bool$$$1, "entry", 0, Thread.currentThread().getId()));
        if ($$$bool$$$1) {
            applyFunction.apply(Arrays.asList(13, "block1", "block1", 0, Thread.currentThread().getId()));
            applyFunction.apply(Arrays.asList(6, 1, "block1", 0, Thread.currentThread().getId()));
            throw returnException;
        }
        applyFunction.apply(Arrays.asList(12, applyFunction.apply(Arrays.asList(7, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(8, n, "entry", 0, Thread.currentThread().getId())).toString()) * factorial(java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(10, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(11, n, "entry", 0, Thread.currentThread().getId())).toString()) - 1, "entry", 0, Thread.currentThread().getId())).toString())), "entry", 0, Thread.currentThread().getId())), "entry", 0, Thread.currentThread().getId()));
        throw returnException;
    }
}