class HelloWorld1 {

    public int given(int n) {
        if (n == 0) {
            return 1;
        } else {
            return 2;
        }
    }

    public void want() {
        boolean $$$bool$$$1 = (boolean) applyFunction.apply(Arrays.asList(1, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(2, n, "entry", 0, Thread.currentThread().getId())).toString()) == 0, "entry", 0, Thread.currentThread().getId()));
        applyFunction.apply(Arrays.asList(0, $$$bool$$$1, "entry", 0, Thread.currentThread().getId()));
        if ($$$bool$$$1) {
            applyFunction.apply(Arrays.asList(3, "block1", "block1", 0, Thread.currentThread().getId()));
            applyFunction.apply(Arrays.asList(4, 1, "block1", 0, Thread.currentThread().getId()));
            throw returnException;
        } else {
            applyFunction.apply(Arrays.asList(5, "block2", "block2", 0, Thread.currentThread().getId()));
            applyFunction.apply(Arrays.asList(6, 2, "block2", 0, Thread.currentThread().getId()));
            throw returnException;
        }
    }
}