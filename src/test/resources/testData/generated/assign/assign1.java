class Assign1 {

    public void given(Foo s) {
        int a = s.length();
    }

    public void want(Foo s) {
        int a = java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(0, java.lang.Integer.parseInt(applyFunction.apply(Arrays.asList(1, s.length(), "entry", 0, Thread.currentThread().getId())).toString()), "entry", 0, Thread.currentThread().getId())).toString());
    }
}

class Foo {

    public int length() {
        return 0;
    }
}