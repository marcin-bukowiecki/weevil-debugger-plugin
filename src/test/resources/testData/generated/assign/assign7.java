import java.util.Arrays;

class Assign7 {

    public void given(String[] args) {
        long ch = Arrays.stream(args).count();
        ch = 100;
        int a = 1, b = 100;
    }

    public void want(String s) {
        long ch = java.lang.Long.parseLong(applyFunction.apply(Arrays.asList(2, Arrays.stream(args).count(), "entry", 0, Thread.currentThread().getId())).toString());
        ch = java.lang.Long.parseLong(applyFunction.apply(Arrays.asList(2, 100, "entry", 0, Thread.currentThread().getId())).toString());
        int a = 1, b = 100;
        applyFunction.apply(Arrays.asList(3, a, "entry", 0, Thread.currentThread().getId()));
        applyFunction.apply(Arrays.asList(4, b, "entry", 0, Thread.currentThread().getId()));
    }
}